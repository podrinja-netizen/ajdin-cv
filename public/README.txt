Put the CV PDF here as:

  ajdin-podrinja-cv.pdf

That is the file the "Download CV (PDF)" button and the
command palette link to. Tip: open /cv in the browser and
print to PDF - that page is built to be exactly that file.
